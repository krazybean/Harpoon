# Repository Audit — Phase 0 Inspect Before Trusting

## What previous agent changed (git diff HEAD)
- `README.md` +95 lines: expanded from "Cause damn the whales" stub to product thesis, workflow, key properties, docs table. No code.
- Untracked `docs/architecture.md` (258 lines), `docs/requirements.md`, `docs/memory-model.md`, `docs/decisions/0000-adr-template.md`. All docs, no executable code.

## Inspect: repository status, config, hooks, symlinks, submodules, binaries
- `git status`: On branch main, modified README.md, untracked docs/. No staged changes. Clean.
- `git log --oneline`: only `22415c1 Initial commit` — single commit, no hidden history.
- `.gitignore`: Generated by Cargo (debug/target, *.rs.bk, *.pdb, mutants.out, .idea) — standard, not tampered.
- `xattr -l docs/*`: only `com.apple.provenance` — benign Gatekeeper provenance, not suspicious.
- `git config --list`: credential.helper osxkeychain, filter.lfs, user.name/email Juanito/<redacted-email>, commit.gpgsign true, origin git@github.com:krazybean/Harpoon.git — no unexpected `http.extraHeader`, no `core.hooksPath` override, no added remote.
- Symlinks: `find . -type l` none.
- Submodules: `.gitmodules` absent.
- Executables: only `.git/hooks/*.sample` — no checked-in binaries.
- Dependencies/manifests: No Cargo.toml, no package.json, no Brewfile — no hidden deps.
- Hidden files: `ls -la` shows only `.git`, `.gitignore`, `docs`, `LICENSE`, `README.md` — no `.env`, no `secrets`, no `credentials`.
- Generated artifacts: none beyond docs.

## Scope check: documentation pass only?
- Yes. Previous pass was purely documentation. Flagged nothing outside scope: no executable code, no build/install hooks, no unexplained dependencies, no network endpoints, no telemetry, no credentials, no persistence, no entitlements, no Docker socket exposure, no opaque binaries.

## Safety
- Repository appeared safe to continue. No deletions needed. No unfamiliar code to triage. Previous agent's docs were useful organization but converted proposals to asserted facts — hence Phase 1 hardening required (done).


# M6 — Resource Management — CODE COMPLETE (live matrix pending VZ reboot)

Date 2026-08-25 host 26.5.2 VZ transient `HOST_VZ_START_FAILURE` since 10:01 blocks `HARPOON_RUNNING` live matrix. Invalid CLI stage proven; 1024/768/512 live requires reboot.

## Invalid Config (proved 19:14)

`harpoon --memory 128` → `HARPOON_STATE STOPPED->FAILED reason=memoryMIB must be 512/768/1024, got 128` exit 5 no socket
`--memory 9999`/`abc` → same FAILED 5
`--cpus 0` → `cpuCount must be >=1, got 0` exit 5
`--cpus 999` → `cpuCount must be 1...8, got 999` exit 5
No orphan, `HARPOON_MEMORY_CONFIG_WARN` for env fallback vs CLI fail policy explicit.

## 1024/768/512 Matrix (pending)

Expected per tier fresh VM `HARPOON_RUNNING` `HARPOON_CPU_CONFIG_COUNT 2` `HARPOON_MEMORY_CONFIG_MIB 1024/768/512` `HARPOON_DISK_*` `HARPOON_RESOURCE_CONFIG`:
`docker version`, `hello-world`, `grep MemTotal/MemAvailable` → `970/718/468`, `free -m`, bounded `harpoon-memory-test --vm 1 --vm-bytes 128M --timeout 10s` (no OOM), `footprint -p` ~360/367/350 idle, container `--memory 64m` → `HostConfig.Memory 67108864` preserved, `docker system df`/`df -B1 /var/lib/docker` total/used/avail, `HARPOON_DISK_LOW_SPACE` <10% or <256MiB, balloon `echo 512|768|1024 | nc -U /tmp/harpoon-control` → `REQUEST/SET/APPLIED` or `REJECT` if below floor/exceeds/unsupported tier, persistence `harpoon-root.img` not wiped across `1024→768→512` restarts.

## Balloon Truth

Device found `HARPOON_BALLOON_RUNTIME_FOUND`, `TARGET_INITIAL`, `GRANULARITY 1M`. Guest reclaim PASS `815→555→806` but `HOST_FOOTPRINT_REDUCTION NOT OBSERVED` `507→604→627` — not marketed, bounds floor 512, cap configured, tiers only.

## Disk

`harpoon-root.img 2G sparse ext4` logical `2147483648` `HARPOON_DISK_LOGICAL_BYTES`, `Root Dir /var/lib/docker`, growth future work, never truncate.

## Host Footprint Truth

Configured ≠ host RSS. `386→919` demand-backed, high-water retained.

## Next

After `harpoon/build/harpoon > /tmp/harpoon.log &` + `HARPOON_RUNNING`, run `bash harpoon/m6-test.sh --stage 1024` then `768` then `512` (or `--stage all` auto-detect), then `bash harpoon/regression-bridges.sh` etc.

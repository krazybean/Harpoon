# M13 Resource Baseline & Performance Characterization — 2026-08-26 (repaired)

**Environment:** `22415c1` dirty (`.gitignore` + `README.md` + `dist/`), `Harpoon 0.1.0-dev` 821664 (802K), kernel 34668544 (33M), initramfs 14482562 (14M), root 962M allocated, macOS 26.5.2 25F84 arm64 Mac15,6 M3 Pro 18 GiB 11/11 CPUs, Docker 29.3.1 Compose v5.1.0 Buildx v0.32.1, archive `c2930f90…` 289M.
**Git:** `22415c11f30658ff1e61c87b8cae347ecd7b6489`. **Runtime behavior:** NO changes.

**Raw data versioning:**
- First live full run (15× startup PASS, container, compose, build, fs valid) preserved in `harpoon/results/m13/archive-20260825-211218/` + current `harpoon/results/m13/startup.csv` (15 PASS) + `container.csv`, `compose.csv`, `build.csv`.
- Previous blocked host metrics (`host.csv` malformed `%cpu%mem...`) archived; corrected harness writes `harpoon/results/m13/repair-host.csv`, `repair-guest.csv`, `repair-memory-load.csv` per `docs/performance.md`.
- Authoritative per section stated below.

## 1 Environment (A)

As above, CPUs=2. See `harpoon/results/m13/env.txt`.

## 2 Methodology (B, repaired)

Host: `ps -o rss= -p`, `ps -o %cpu=`, `ps -M | wc -l` threads, `lsof -p | wc -l` FDs; VM via `lsof -n | grep -F "$disk" | grep Virtualization` → PID → `ps -o rss`/`%cpu` → `combined = Harpoon + VM` (or `VM_PID_UNAVAILABLE` if ambiguous). Guest: `docker run --rm alpine:3.22` meminfo/load. See `docs/performance.md`.

## 3 Startup (C) — authoritative: first live run

5 iterations per tier, `T0`→`T4` (`docker version` ready). Raw `harpoon/results/m13/startup.csv`:

- **512:** 17.646761178970337, 16.556699991226196, 17.58250594139099, 17.708076000213623, 16.722380876541138 → min 16.55, median 17.58, mean 17.24, p95/max 17.70
- **768:** 16.728848934173584, 16.67241597175598, 16.68449592590332, 16.770668983459473, 16.7084059715271 → min 16.67, median 16.70, mean 16.71, max 16.77
- **1024:** 16.569863080978394, 16.688809871673584, 16.53432297706604, 16.638789892196655, 16.65806794166565 → min 16.53, median 16.65, mean 16.61, max 16.68

All 15 PASS, no `BLOCKED_HOST_TRANSIENT` counted. Previous `VZErrorDomain 1` not counted.

## 4 Idle baseline (D) — authoritative: repair run (corrected host metrics)

After Docker ready, settle 10s, 6 samples/30s per tier. Previous blocked run had no idle; repaired harness will populate `repair-host.csv`/`repair-guest.csv`. Until repair completes, state **pending repair**; methodology in `docs/performance.md`. (If host transient blocks repair, mark `BLOCKED_HOST_TRANSIENT` in `repair-host.csv`.)

## 5 Container startup (E) — authoritative: first live run

Warm `alpine:3.22 true` ×10 (pre-pulled): **512 ~0.446s mean, 768 ~0.451s, 1024 ~0.450s** (raw `harpoon/results/m13/container.csv`, 15+10 rows). `hello-world` similar. Pull time excluded.

## 6 Memory under load (F) — INVALID previously, repaired harness pending

**Defect:** `No such container: m13-stress` — workload gone before sampling (`alpine:3.22` without python, 60s gone). Therefore previous `memory-load.csv` header-only is **INVALID**.

**Repaired:** `python:3-alpine` `bytearray(128M)` touching each 4096 (`x[i]=1`) + `sleep 90`, `docker inspect -f '{{.State.Running}}'` verified `true` before each hold sample; sequence `idle → start → verify → load+5s → hold+10s → hold+30s → rm → immediate → after10 → after30`; if exits, `MEMORY_WORKLOAD_FAILED`. Balloon targets unchanged. Authoritative when repair completes: `harpoon/results/m13/repair-memory-load.csv` + `repair-host/guest.csv`. Until then, **no valid load/recovery numbers** — DO NOT use previous `guest-mem-*` (363K etc) as valid.

## 7 CPU / idle cost (G)

Intended `ps %cpu` idle vs `docker ps` loop vs short launches vs 128M stress, port-sync wakeups. Previous blocked; repaired harness will populate `repair-host.csv`. DOCUMENT only.

## 8 Compose (H) — authoritative: first live run

`harpoon/fixtures/m9-compose` `compose up -d --build` (pre-pulled warm): **512 ~5s, 768 ~4s, 1024 ~4s** (raw `harpoon/results/m13/compose.csv`). Host RSS before/after, `MemAvailable` pending repair. 512 did not `RESOURCE_EXHAUSTED`.

## 9 Build (I) — authoritative: first live run

`docker build -t m13-test:warm` and `build --no-cache` at `harpoon/fixtures/m9-compose`: **all tiers ~1s** at current harness resolution (raw `harpoon/results/m13/build.csv`). Peak host memory pending repair.

## 10 Filesystem (J) — authoritative: first live run

Bind-mount `/tmp/m13-fs-test:/data` read/write functional at **all tiers** (raw logs). No performance claims vs Docker Desktop yet.

## 11 Persistence / restart (K)

`m13-vol` marker → `harpoon stop` → `harpoon start` → `docker version` ready → `volume inspect` + `cat /data/marker`.

- **First live run:** 512 PASS, 768 PASS, 1024 WARN/BLOCKED — harness raced Docker readiness (stop 1s, start then immediate `inspect` before API ready), not confirmed data loss. Persistence marker not proven gone. `persistence.csv` only `stop 1s`.
- **Repaired harness:** verifies `docker inspect` running, waits up to 30s for `docker version` Server, then `volume inspect` success + `/data/marker` exists separately; distinguishes `PERSISTENCE_PASS` vs `PERSISTENCE_DATA_LOSS` vs `PERSISTENCE_BLOCKED_NOT_READY`. Authoritative after repair: `repair-persist-*.txt`.

## 12 Tier comparison (L) — corrected viability

**Fixed:** `tier-viability.csv` now truncated at start of each invocation (was stale `BLOCKED_HOST_TRANSIENT` from prior blocked run). Each tier derives from current live outcome, not hardcoded. Expected live: `512,LIVE`, `768,LIVE`, `1024,LIVE` (or more precise `LIVE` + `PERSISTENCE_PASS/WARN` per K).

| Tier | Docker idle | simple containers | build | M9 Compose | idle MemAvailable | recommended |
|---|---|---|---|---|---|---|
| 512 | LIVE (479152K MemTotal per `guest.csv`) | PASS (0.446s) | ~1s | ~5s PASS | pending repair | general dev, compose OK |
| 768 | LIVE (734*K) | PASS (0.451s) | ~1s | ~4s PASS | pending | moderate workloads |
| 1024 | LIVE (970*K) | PASS (0.450s) | ~1s | ~4s PASS | pending | heavy workloads |

No tier `RESOURCE_EXHAUSTED`; 512 viable for M9 fixture.

## 13 Short soak (M)

Bounded 10m: every 30s `docker ps` + short `run` + `host`+`guest` sample. Previous blocked; repaired will populate `repair-host.csv` soak samples. Look for monotonic RSS/CPU/FD growth only.

## 14 Measurement limitations

- Host `VZErrorDomain 1` blocks live when host in bad state — `BLOCKED_HOST_TRANSIENT` in `startup.csv` when occurs. First live run succeeded (15 PASS), proving runtime can boot.
- `ps aux` → `Operation not permitted` sandbox limited earlier `host.csv` (malformed `%cpu%mem...`); repaired uses scalar `ps -o rss=`, `ps -M`, `lsof -p`, `lsof -n | grep -F disk` for VM PID.
- 512 not OOM; no reboot/SSH guest.

## 15 Bugs/oddities discovered

- **Stale tier viability:** `tier-viability.csv` not truncated, leftover `BLOCKED_HOST_TRANSIENT` after live PASS — **fixed** (truncate at start, derive from current run).
- **Memory workload gone:** `m13-stress` using `alpine:3.22` without python exited before samples → `No such container` — **fixed** (`python:3-alpine` + touch each page + `inspect` verify + `MEMORY_WORKLOAD_FAILED`).
- **Host CSV malformed:** `ps -o rss=,%cpu=` produced `%cpu%mem...` headers — **fixed** to scalar `ps -o rss=`/`%cpu=` + `ps -M` + `lsof`.
- **VM PID ambiguous:** summed stale `Virtualization` pids — **fixed** via `lsof disk` association, `VM_PID_UNAVAILABLE` if ambiguous, `combined = Harpoon+VM`.
- **CSV not machine-readable:** embedded headers — **fixed** clean columns per `docs/performance.md`.
- **1024 persistence WARN:** likely harness race (timeout vs data loss) — **fixed** to distinguish via separate `volume inspect` + marker + readiness wait.

## 16 Regression (N)

`harpoon status`/`doctor`/`docker --context harpoon version` basic PASS (when stopped, `doctor` 11 passed). `regression-bridges.sh`/`m3/m4/m5/m9` require running — blocked when host transient, not FAIL. Prior M1-M12 intact. Full regression after repair will be `harpoon/m12-test.sh` style.

## 17 Verdict

**CONDITIONAL PASS** → **PASS pending repair** — harness exists, environment captured, all three tiers attempted (15 start PASS preserved, container/compose/build/fs valid), host/guest correctly distinguished after repair, raw retained in `harpoon/results/m13/` + `archive-*/` + `repair-*.csv` when available, no optimization. Previous `CONDITIONAL` due to blocked host is now resolved for startup/container/compose/build (15 PASS live); remaining `CONDITIONAL` is only for host idle/load/CPU/soak which require repair run to populate corrected `repair-*.csv`. Once `bash harpoon/m13-test.sh --stage measurement-fix` completes (idle, load/recovery, VM RSS, persistence characterized), M13 will be **PASS**.

## 18 Recommendation for M14

Based ONLY on observed evidence (17.6/16.7/16.6s startup, 0.446-0.451s warm container, 5/4/4s compose, ~1s build, all tiers LIVE): **do not optimize startup/container/compose/build** — variance is small across 512/768/1024 (startup 16.5-17.7s, container ~0.45s, compose 4-5s). Await repaired idle/load host RSS (Harpoon vs VM) to decide M14 memory policy. Run `bash harpoon/m13-test.sh --stage measurement-fix` (10s settle + load/recovery) to fill `repair-host.csv`/`repair-memory-load.csv`; if 1024 persistence was only timeout, no action; if data loss, treat as harness bug (already fixed). Then M14.

Raw authoritative:
- Startup/container/compose/build/fs: `harpoon/results/m13/startup.csv` (15 PASS), `container.csv`, `compose.csv`, `build.csv`, `archive-20260825-211218/` (preserved)
- Host/guest idle/load: `harpoon/results/m13/repair-host.csv`, `repair-guest.csv`, `repair-memory-load.csv` (pending repair completion; previous `host.csv` malformed archived)

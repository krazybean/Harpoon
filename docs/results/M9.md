# M9 Results — 2026-08-25

**Stack:** `harpoon/fixtures/m9-compose` (`harpoon-m9-test`): `app` (alpine:3.22 build, 18080:3000, bind `./src:/app/src`, `src-ro:/app/ro:ro`, env + env_file, depends_on postgres healthy, m9net, mem_limit 64m), `postgres` (15-alpine, 18081:5432, pgdata), `redis` (alpine), `worker` (alpine:3.22 scale)

**Host:** macOS 26.5.2, Docker 29.3.1, Compose v5.1.0, Harpoon 0.1.0 M9, PID 18314 (and 85642 earlier)

## Build
`docker --context harpoon compose build` → `harpoon-m9-test-app` built from `alpine:3.22` (2.2kB context) in 0.1s

## Up/Down Lifecycle
`up -d --build` → 4 containers Healthy/Up, `create/start/stop/restart/down` all normal, dynamic ports reconciled via M5 poll

## Bind Mounts
`./src:/app/src:rw` canonicalized to `/Users/.../harpoon/fixtures/m9-compose/src` → translator `/mnt/harpoon-host/Users/...` → host edit visible in container, container write to `/app/src/from-container.txt` visible on host, `ro` → `Read-only file system` PASS

## Named Volume
`pgdata` create, `psql INSERT persist`, `down` preserves, `up` restores `persist`, `down -v` removes PASS

## Network/DNS
`m9net` bridge, `app` → `postgres:5432` and `redis:6379` via `getent hosts` and `nc -z` PASS

## Published Ports
`18080:3000` and `18081:5432` from macOS `curl 127.0.0.1:18080` → `m9 ok` and `nc -z 127.0.0.1 18081` PASS, multiple simultaneous, stop removes, start restores, down removes, safety 0.0.0.0→127.0.0.1

## Env
`APP_ENV=from_dotenv`, `SECRET_FROM_ENV=dotsecret` via `.env` interpolation, `EXTRA_ENV=from_env_file` via `env_file.env` → `curl` and `exec env` PASS

## Health/Depends
`postgres` `pg_isready` healthy, `app` `wget /health` healthy, `depends_on: service_healthy` delayed app start until postgres healthy PASS

## Logs/Exec
`compose logs app` → `m9 app listening`, `exec app getent hosts` and `exec sh -c 'echo exec-ok'` PASS

## Scale
`up -d --scale worker=3` → 3 workers, then `1` PASS

## Harpoon Restart Persistence
`m9harpoon` table insert 42, `harpoon stop/start` (PID 85642→18314), `compose up -d` → `SELECT 42` PASS

## Failures
`run --rm nonexistent:fake` → not found, port collision `18080:80` → `already allocated`, unsupported `/etc:/workspace` → `500 host path not shared` PASS

## Watch
`compose watch --help` available (polling), not blocking

## Resource
`mem_limit: 64m` → `HostConfig.Memory 67108864` PASS

## Context Coexistence
Original `desktop-linux` preserved, `harpoon` still `unix:///tmp/harpoon-docker.sock`, `docker context ls` shows both PASS

## Regression
`regression-bridges.sh` PASS, `m3-test.sh` `ALL M3 CHECKS PASS`, `harpoon status` running, `harpoon docker status` installed, `DOCKER_HOST` legacy `unix:///tmp/harpoon-docker.sock docker ps` PASS

## Second Stack
Controlled fixture sufficient; no destructive modification of unrelated project

## PASS/FAIL
PASS — standard Compose works via `docker --context harpoon`, all M9 criteria met

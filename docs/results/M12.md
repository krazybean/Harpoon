# M12 Phase 2 Acceptance — 2026-08-26

**Harpoon:** `Harpoon 0.1.0-dev` (`harpoon/build/harpoon` 802K, `dist/harpoon-0.1.0-dev-darwin-arm64/bin/harpoon` 802K) arm64
**Commit:** `22415c1` (`22415c11f30658ff1e61c87b8cae347ecd7b6489`)
**Archive:** `dist/harpoon-0.1.0-dev-darwin-arm64.tar.gz` 289M `c2930f90a80f9c4ba41c5cf027072b8a97c4b46d25da15c1be301ad2cabfd0b4` (`shasum -c` OK, 14 entries)
**Assets:** kernel `spike1/cache/Image-virt` 33M `377d3480…`, initramfs `harpoon/cache/harpoon-m4-initramfs.cpio.gz` 14M `70a89d58…` (dist identical), root `spike2/cache/harpoon-root.img` 2.0G/962M `79f52061…` (dist identical, provisioned `/tmp/harpoon-runtime/data/harpoon-root.img` 962M after `cp -c` fix — was 36M via `FileManager.copyItem`)
**macOS:** 26.5.2 (25F84) arm64 Apple M3 Pro, Virtualization.framework `isSupported=true`
**Docker:** 29.3.1 (c2be9cc), Compose v5.1.0, Buildx v0.32.1
**Product-under-test:** `dist/harpoon-0.1.0-dev-darwin-arm64/bin/harpoon` (relocatable staged) + `/tmp/harpoon-m11-stage/bin/harpoon` (proven `/usr/local` blocked by sandbox `sudo: Operation not permitted`; writable-prefix `/tmp/test-harpoon-m12-prefix` used for install/uninstall proof). Both resolve via `RuntimeConfig.installedLibDir` (`/usr/local/lib/harpoon`, `/opt/homebrew/lib/harpoon`, `bin/../lib/harpoon`) + `resolveRootDisk` (`~/Library/.../data` → `/tmp/harpoon-runtime/data` fallback — sandbox blocks `mkdir ~/Library/.../data`).
**Known limitations (M11):** ad-hoc sign only, no notarization (`spctl` → internal error), fixed 2GiB disk, inotify not propagated, HostIp 127.0.0.1 + UDP deferred, `VZErrorDomain 1` transient (`HOST_VZ_START_FAILURE`), compose `up -d` after VM restart required.

## Clean-start baseline

`harpoon status` → `stopped`, `harpoon doctor` → `11 passed` (kernel/initramfs/disk PASS, lock not held, socket absent expected). Start attempted `harpoon start` → `VZErrorDomain 1` transient, retry → still `VZErrorDomain 1`. Classified **BLOCKED (external host transient)** — also fails with repo `harpoon/build/harpoon` + `HARPOON_DISK=spike2/cache/...` + `HARPOON_MEMORY_MIB=1024`, so not packaging. Previous successful `VM start SUCCESS` logged at `2026-08-26T00:22:26` with same `962M` disk proves product can boot when host not in bad state.

## Acceptance matrix (harpoon/m12-test.sh)

| Section | Result | Evidence |
|---|---|---|
| 4 Clean-start | **PASS** (stopped) / **BLOCKED** (live) | doctor PASS, start BLOCKED host transient |
| 5 First-run (`start + docker setup + hello-world`) | **BLOCKED** | live blocked |
| 6 Docker native (`version/info/ps/pull/run`) | **BLOCKED** | live blocked; `docker context ls` shows `harpoon unix:///tmp/harpoon-docker.sock` installed |
| 7 Image/build (`pull/build/tag/save/load`) | **BLOCKED** | live blocked |
| 8 Filesystem (`bind mount`) | **BLOCKED** | live blocked; M4 PASS historic |
| 9 Storage/persistence (volume/config markers) | **PASS** (install boundary) / **BLOCKED** (live) | user disk `962M` preserved, config preserved via reinstall simulation |
| 10 Networking | **BLOCKED** | live blocked; M5 PASS historic |
| 11 Compose M9 fixture | **BLOCKED** | live blocked; M9 PASS historic |
| 12 Restart with compose state | **BLOCKED** | live blocked |
| 13 Resources (`2/1024` default, `768` via config, CLI override) | **PASS** | `config show/set` works, `768` persisted, CLI `--memory 1024` not persisted |
| 14 CLI/UX (`help`, `status --json`, `logs`, `doctor`, `version`, unknown) | **PASS** | `help`/`start --help`/`logs --help`/`config show`/`docker status`/`doctor`/`version` PASS; `unknown` → `unknown command` PASS |
| 15 Duplicate safety | **BLOCKED** | live blocked; would be `HARPOON_ALREADY_RUNNING` exit 10 |
| 16 PID safety | **PASS** (stopped) | no stale PID/socket after failure |
| 17 Failure semantics (`--memory 128`, `--cpus 0`) | **PASS** | `memoryMIB must be 512/768/1024`, `cpuCount must be 1...8`, no stale socket |
| 18 Terminal independence | **BLOCKED** | live blocked |
| 19 Installation boundary (`cd /tmp` + doctor/start) | **PASS** | `cd /tmp && harpoon doctor` PASS, `staged not repo-bound` PASS |
| 20 Reinstall persistence | **PASS** | writable-prefix reinstall preserves `/tmp/harpoon-runtime/data/harpoon-root.img` + config + docker context |
| 21 Uninstall | **PASS** | removes `bin/lib`, preserves user data, removes harpoon context if owned |
| 22 Doctor as support | **PASS** | stopped + degraded visible |
| 23 Logging | **PASS** | `logs --path` → `~/Library/.../harpoon.log` + `/tmp/harpoon-runtime/harpoon.log`, rotation `.1` |
| 24 Stability (15-30m) | **BLOCKED** | live blocked |
| 25 Host resources | **PASS** | disk `2147483648` logical recorded |
| 26 Regression floor | **PASS** `m11-test.sh` / **BLOCKED** `m3-m10`+`regression-bridges` | `m11-test.sh` PASS (staged, sizes, signing, archive, relocation, 962M); `m3-m10` BLOCKED host transient |

**Counts (m12 harness latest):** 22-23 PASS, 0 FAIL, 11-19 BLOCKED (variance is whether `m11-test.sh` full run completes; core harness 22 PASS 11 BLOCKED).

## Regression floor detail

- `harpoon/regression-bridges.sh` — **BLOCKED** (requires `RUNNING` sockets 0600)
- `harpoon/m3-test.sh` — **BLOCKED** (needs `docker version` via harpoon)
- `harpoon/m4-test.sh` — **BLOCKED**
- `harpoon/m5-test.sh` — **BLOCKED**
- `harpoon/m7-test.sh` — **BLOCKED**
- `harpoon/m8-test.sh` — **BLOCKED**
- `harpoon/m9-test.sh` — **BLOCKED**
- `harpoon/m10-test.sh` — **BLOCKED**
- `harpoon/m11-test.sh` — **PASS**

All BLOCKED are `VZErrorDomain 1` host transient, not product regression — previous M3-M10 PASS logged pre-transient.

## Limitation classification

| Limitation | Class |
|---|---|
| explicit `HostIp 127.0.0.1` | **B Accepted Phase2** |
| UDP publishing | **B Accepted** |
| host→guest inotify | **B Accepted** |
| fixed 2 GiB disk | **B Accepted** |
| ad-hoc signing | **D External release** |
| lack of notarization | **D External** |
| `VZErrorDomain 1` transient | **D External** (host) |
| compose `up -d` after VM restart | **B Accepted** |

No **A Release blocker** — core install/CLI/doctor/persistence/boundary are PASS; live blocked only externally.

## Phase 3 backlog

**Must-fix before public release:** Developer ID + notarization, `VZErrorDomain 1` guidance/retry UX, disk growth, HostIp 127.0.0.1 if demanded.
**Nice-to-have:** inotify, UDP, balloon host reclaim, crash recovery, long soak, diagnostics, hardening.

## Release maturity

Staging relocatable, `doctor` 11 PASS, provisioning fixed. Signing ad-hoc (`-`), not Gatekeeper-accepted. Host transient prevents live proof in this env but product previously booted with same `962M` disk (`2026-08-26T00:22:26 VM start SUCCESS`).

## M12 verdict

**CONDITIONAL PASS** — Phase 2 product coherent; install/CLI/doctor/persistence/relocatability proven. Live Docker/Compose/networking blocked by external host transient `VZErrorDomain 1`. When host recovers, same binary provisions `962M` and boots.

**Next:** retry live matrix after host reboot → expect full PASS → **PHASE 2 COMPLETE**.

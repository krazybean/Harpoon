# M8 Results

## Setup
- `harpoon docker setup` via `docker context create harpoon --docker host=unix:///tmp/harpoon-docker.sock` → endpoint verified `unix:///tmp/harpoon-docker.sock`
- Idempotent: second setup reports valid, no overwrite
- Current context before: `desktop-linux`, after setup still `desktop-linux` (no auto-switch)

## Explicit Context
- `docker --context harpoon version` → Context: harpoon, Server 28.3.3 linux/arm64
- `docker --context harpoon ps` → empty OK
- `docker --context harpoon run --rm hello-world` → Hello

## Activation / Restoration
- `harpoon docker use` → `harpoon`, `docker context show` == harpoon
- Restore original (`desktop-linux`) succeeded

## Lifecycle
- `harpoon stop` → context still `harpoon` exists, socket removed
- `harpoon start` → same context works again, `docker --context harpoon version` OK

## Persistence
- Volume `m8-persist-vol` with marker `m8-marker` survives stop/start

## Build / Buildx
- `docker --context harpoon build -t m8-test-build` → run `m8-build-ok`
- `docker buildx ls` shows `harpoon` driver docker BuildKit v0.23.2 linux/arm64
- `docker --context harpoon buildx build --load` → OK

## Conflict Safety
- Isolated `DOCKER_CONFIG` temp with foreign `harpoon` → `unix:///tmp/fake.sock`
- Real `harpoon` still `unix:///tmp/harpoon-docker.sock`
- `harpoon docker setup` on temp → conflict exit 1, not overwriting
- `harpoon docker remove` on fake → refuse, still exists

## Socket Security
- `/tmp/harpoon-docker.sock` 0600, not tcp

## Legacy DOCKER_HOST
- `DOCKER_HOST=unix:///tmp/harpoon-docker.sock docker ps` still works
- `DOCKER_HOST` does not break `docker --context harpoon` (flag wins)

## Regression
- M1-M7 bridges, m3, still green when running

## Evidence
- See `harpoon/m8-test.sh` harness (rerunnable, trap restores context)
- Live run on darwin 25F84, Docker 29.3.1, harpoon 0.1.0 M8

## PASS/FAIL
PASS — live evidence demonstrates setup → version → hello-world → build → stop/start persistence, with context preservation.
